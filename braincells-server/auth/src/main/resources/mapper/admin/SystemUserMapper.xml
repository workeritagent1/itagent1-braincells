<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.worker.itagent1.auth.oauth.dao.SystemUserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.worker.itagent1.auth.oauth.entity.SystemUser">
        <id column="user_id" property="userId" />
        <result column="user_name" property="userName" />
        <result column="password" property="password" />
        <result column="status" property="status" />
        <result column="roles" property="roles" />
        <result column="dept_id" property="deptId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        user_id, user_name, password, status, roles, dept_id
    </sql>

    <!-- 根据用户名获取认证信息 -->
    <select id="getUserByUserName" resultMap="BaseResultMap">
        SELECT t1.*
        FROM
            sys_user t1
        WHERE
            t1.username = #{username} and t1.deleted=0
    </select>
</mapper>
